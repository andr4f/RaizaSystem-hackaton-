// TODO: timeline de eventos de trazabilidad — GET /api/v1/traceability/lots/{lotId}/events
// También permite crear eventos — POST /api/v1/traceability/events
const TraceabilityEvents = ({ lotId }) => {
  return null
}

export default TraceabilityEvents
