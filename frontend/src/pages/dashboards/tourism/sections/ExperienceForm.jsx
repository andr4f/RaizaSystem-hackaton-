// TODO: formulario para crear experiencia — POST /api/v1/tourism/experiences
// Campos: name, description, location, duration, price, capacity, operatorId
const ExperienceForm = ({ onSuccess }) => {
  return null
}

export default ExperienceForm
