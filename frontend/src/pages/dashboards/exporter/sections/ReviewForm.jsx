// TODO: formulario para crear nueva review — POST /api/v1/exporters/reviews
// Campos: lotId, rating, comments, exportDate, destination
const ReviewForm = ({ onSuccess }) => {
  return null
}

export default ReviewForm
